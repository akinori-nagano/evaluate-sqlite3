TARGET = sqlite3 testmain sample

#--------------------------------------------------------------------
all:
	@for ttt in $(TARGET); do \
		echo "##### make $$ttt #####"; \
		$(MAKE) -f Makefile.$$ttt $@ || exit; \
	done
#--------------------------------------------------------------------
clean:
	rm -f ./obj/*.o ./sample
#--------------------------------------------------------------------
